{
  "name": "AI Email Agent Workflow",
  "description": "Automated email drafting and sending system",
  "triggers": [
    {
      "type": "webhook",
      "config": {
        "method": "POST",
        "path": "/email-request"
      }
    }
  ],
  "nodes": [
    {
      "name": "Parse Request",
      "type": "function",
      "config": {
        "jsCode": "const { user_input, sender_name, company } = $input; return { variables: { user_input, sender_name, company } };"
      }
    },
    {
      "name": "AI Email Generation",
      "type": "http_request",
      "config": {
        "method": "POST",
        "url": "https://api.anthropic.com/v1/messages",
        "headers": {
          "Content-Type": "application/json",
          "x-api-key": "{{AI_API_KEY}}"
        },
        "body": {
          "model": "claude-3-sonnet-20240229",
          "messages": [
            {
              "role": "user",
              "content": "As {{sender_name}} from {{company}}, create professional email for: {{user_input}}. Respond ONLY with: {\"to\":\"\",\"subject\":\"\",\"body\":\"\"}"
            }
          ],
          "temperature": 0.3
        }
      }
    }
  ]
}
